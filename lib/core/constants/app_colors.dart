import 'package:flutter/material.dart';

class AppColors {
  // Brand Colors
  // Brand Colors
  static const Color primaryBlue = Color(0xFF366FED); // Main Brand Color
  static const Color primaryLight = Color(0xFF5E8BF1); // Lighter shade for hover/accents
  static const Color primaryDark = Color(0xFF2B59BD); // Darker shade for pressed states
  static const Color navyBlue = Color(0xFF0F172A); // Web Footer/Headings (Slate-900)
  static const Color webHeader = Color(0xFFFFFFFF); // Web Header is White
  
  // Backgrounds
  static const Color scaffoldBackground = Color(0xFFF1F5F9); // Web Dashboard Background (Slate-100)
  static const Color white = Color(0xFFFFFFFF);
  static const Color sidebarBackground = Color(0xFFFFFFFF);
  static const Color lightGrey = Color(0xFFE2E8F0); // Slate-200 for Borders
  
  // Accents
  static const Color gold = Color(0xFFC5A059); // Brand Gold
  static const Color greenAccent = Color(0xFF10B981);
  static const Color blueAccent = Color(0xFF3B82F6);

  // Text
  static const Color textPrimary = Color(0xFF1E293B); // Slate-800
  static const Color textSecondary = Color(0xFF64748B); // Slate-500
  static const Color grey = Color(0xFF94A3B8); // Slate-400

  // Feedback
  static const Color error = Color(0xFFEF4444);
}
